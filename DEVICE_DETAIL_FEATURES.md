# 📱 Device Detail Page - Features

## 🎯 เมื่อคลิกที่ Device Card

จะไปที่หน้า: `/dashboard/device/{deviceId}`

---

## 📋 Features ที่มีในหน้า Device Detail

### 1. **Device Status Card**
แสดงข้อมูลสถานะของ device:
- 🔋 Battery: เปอร์เซ็นต์แบตเตอรี่
- 📶 WiFi: สถานะ WiFi (On/Off)
- ⚡ Status: สถานะ device (ONLINE/OFFLINE/IN_USE/AVAILABLE)
- 🔒 Kiosk Mode: สถานะ Kiosk Mode (On/Off)

### 2. **Location Card** (ถ้ามี GPS)
- 📍 GPS Coordinates: ละติจูด, ลองจิจูด
- 🗺️ Map View: แสดงแผนที่ (Coming Soon - placeholder)

### 3. **Control Panel**
ปุ่มควบคุม device:
- 🔒 **Lock** - ล็อคหน้าจอ
- 🔄 **Restart** - รีสตาร์ท device
- 📶 **WiFi On** - เปิด WiFi
- 📶 **WiFi Off** - ปิด WiFi
- 🟦 **Enable Kiosk** - เปิด Kiosk Mode
- 🟦 **Disable Kiosk** - ปิด Kiosk Mode

### 4. **Action History**
แสดงประวัติการทำงาน:
- รายการ commands ที่ส่งไป
- วันที่/เวลา
- ผู้ใช้งาน (ถ้ามี)

### 5. **Quick Actions** (Sidebar)
- 📤 **Borrow Device** - เบิกเครื่อง
- 📥 **Return Device** - คืนเครื่อง

### 6. **Latest Metrics** (Sidebar)
แสดง metrics ล่าสุด:
- 💻 CPU Usage (%)
- 🧠 Memory Usage (%)
- 💾 Storage Usage (%)

### 7. **Realtime Updates**
- WebSocket เชื่อมต่ออัตโนมัติ
- อัปเดตข้อมูล realtime เมื่อ device ส่งข้อมูลมา

---

## 🔄 Flow เมื่อคลิก Device

1. **Navigate** → ไปที่ `/dashboard/device/{deviceId}`
2. **Check Auth** → ตรวจสอบ token
3. **Fetch Device** → เรียก API `/api/device/{id}` เพื่อดึงข้อมูล device
4. **Connect WebSocket** → เชื่อมต่อ WebSocket สำหรับ realtime updates
5. **Display** → แสดงข้อมูล device, metrics, history

---

## ⚠️ สิ่งที่อาจเกิดขึ้น

### ถ้า Device ไม่พบ
- แสดง "Device not found"
- มีปุ่ม "Back to Dashboard"

### ถ้า Loading
- แสดง loading spinner
- รอจนกว่าจะโหลดข้อมูลเสร็จ

### ถ้าไม่มี GPS
- Location Card จะไม่แสดง

### ถ้าไม่มี Metrics
- Latest Metrics จะไม่แสดง

---

## 🎮 การใช้งาน

### ส่ง Command
1. กดปุ่มใน Control Panel (เช่น Lock, Restart)
2. ระบบจะส่ง command ผ่าน MQTT ไปยัง device
3. แสดง success/error message

### เบิก/คืนเครื่อง
1. กด "Borrow Device" หรือ "Return Device"
2. กรอก reason (ถ้าเบิก)
3. ระบบจะบันทึกใน database

---

## 📝 API Endpoints ที่ใช้

- `GET /api/device/{id}` - ดึงข้อมูล device
- `POST /api/device/{id}/command` - ส่ง command
- `POST /api/device/{id}/borrow` - เบิกเครื่อง
- `POST /api/device/{id}/return` - คืนเครื่อง

---

## ✅ สรุป

เมื่อคลิกที่ device card:
- ✅ ไปที่หน้า Device Detail
- ✅ แสดงข้อมูล device ครบถ้วน
- ✅ แสดง Control Panel สำหรับควบคุม device
- ✅ แสดง History และ Metrics
- ✅ เชื่อมต่อ WebSocket สำหรับ realtime updates

