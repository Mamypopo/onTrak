# 📱 คู่มือการเข้าถึงจากมือถือ

## 🔍 ปัญหาที่พบ

QR Code ที่สร้างใน PDF ใช้ `localhost:3001` ซึ่งมือถือเข้าถึงไม่ได้

## ✅ วิธีแก้ไข

### 1. หา IP Address ของเครื่อง

**Windows:**
```bash
ipconfig
# ดูที่ IPv4 Address (เช่น 192.168.1.100)
```

**Mac/Linux:**
```bash
ifconfig
# หรือ
ip addr
```

### 2. เข้าถึง Admin ผ่าน IP Address

แทนที่จะใช้ `http://localhost:3001/admin` ให้ใช้:
```
http://192.168.1.100:3001/admin
```
(เปลี่ยน `192.168.1.100` เป็น IP address ของคุณ)

### 3. สร้าง QR Code ใหม่

เมื่อเข้าถึง Admin ผ่าน IP address แล้ว:
- QR Code ที่สร้างจะใช้ IP address อัตโนมัติ
- มือถือจะสามารถสแกนและเข้าถึงได้

---

## 🎯 ขั้นตอนการใช้งาน

1. **หา IP Address:**
   - Windows: เปิด Command Prompt → พิมพ์ `ipconfig`
   - Mac/Linux: เปิด Terminal → พิมพ์ `ifconfig`

2. **เข้าถึง Admin ผ่าน IP:**
   ```
   http://[IP_ADDRESS]:3001/admin
   ```
   เช่น: `http://192.168.1.100:3001/admin`

3. **เปิดโต๊ะและสร้าง QR Code:**
   - QR Code จะใช้ IP address อัตโนมัติ
   - มือถือสามารถสแกนและเข้าถึงได้

4. **ทดสอบ:**
   - ใช้มือถือสแกน QR Code
   - ควรเปิดหน้าเว็บได้ปกติ

---

## ⚠️ หมายเหตุ

- **ต้องอยู่ในเครือข่ายเดียวกัน:** มือถือและคอมต้องเชื่อมต่อ WiFi เดียวกัน
- **Firewall:** ตรวจสอบว่า Firewall ไม่บล็อก port 3001
- **Production:** สำหรับใช้งานจริงควรใช้ domain name แทน IP address

---

## 🔧 วิธีแก้ไขถ้ายังไม่ได้

### ตรวจสอบ Server
```bash
# ตรวจสอบว่า server ฟังที่ 0.0.0.0
# ดูใน server.js ควรเป็น:
hostname = '0.0.0.0'
```

### ตรวจสอบ Firewall
- Windows: เปิด Windows Firewall → Allow app → Node.js
- Mac: System Preferences → Security → Firewall

### ทดสอบการเชื่อมต่อ
```bash
# จากมือถือ เปิด browser และลอง:
http://[IP_ADDRESS]:3001
```

---

## 📝 ตัวอย่าง

ถ้า IP address ของคุณคือ `192.168.1.100`:

1. เข้าถึง Admin: `http://192.168.1.100:3001/admin`
2. เปิดโต๊ะ → สร้าง QR Code
3. QR Code จะมี URL: `http://192.168.1.100:3001/session/[id]`
4. มือถือสแกน → เปิดหน้าเว็บได้

